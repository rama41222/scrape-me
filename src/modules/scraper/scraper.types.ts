export interface MetaDataInput {
  links: number;
  anchors: number;
  images: number;
  last_fetch: string;
  site: string;
}

export interface MetaData {
  num_links: number;
  images: number;
  last_fetch: string;
  site: string;
}
